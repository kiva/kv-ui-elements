import { Canvas, Meta, Story, Controls } from '@storybook/addon-docs/blocks';
import * as KvCompactLoanCardStories from './KvCompactLoanCard.stories.js';

<Meta title="Loan Display/KvCompactLoanCard" component={KvCompactLoanCardStories.Component} />

# KvCompactLoanCard

A compact loan card component that displays essential borrower and loan information in a space-efficient layout, designed for loan listings and collections on the Kiva platform.

## Component Overview

KvCompactLoanCard provides a condensed view of loan information including borrower image, name, location, loan purpose, funding progress, and lending actions. It supports multiple display modes (standard, light view, post-goal, and light detailed), handles loading states gracefully, and integrates with Kiva's basket and tracking systems. The component is highly configurable to support various use cases like matched loans, team picks, and US direct loans.

<Story of={KvCompactLoanCardStories.ComponentOverview} />

## Table of Contents

- [Variations](#variations)
- [Usage Information](#usage-information)
- [Behavior](#behavior)
- [Anatomy](#anatomy)
- [Specs](#specs)
- [Best Practices](#best-practices)
- [Accessibility](#accessibility)
- [Component Properties + Demo](#component-properties)
- [Code Examples](#code-examples)

---

## Variations

The component supports numerous variations to handle different loan states and display contexts:

- **Standard View**: Full-featured card with tags, callouts, and lending CTA on dark backgrounds
- **Light View**: Simplified, compact version for lighter UI contexts with smaller dimensions; the loan use statement ends in a "read more" link and can be capped at a set number of lines, in which case the link only shows when the statement is cut short
- **Loading States**: Skeleton placeholders while loan data is being fetched
- **Matched Loans**: Displays matching partner information and special styling
- **Almost Funded**: Shows loans near completion with appropriate progress indicators
- **In Basket**: Indicates when a loan is already in the user's cart
- **Team Pick**: Special designation for team-selected loans
- **US Direct Loans**: Adapted layout for US-based borrowers with different location formatting
- **Business View**: Alternative display showing business name and website links
- **Post Goal Variant**: A compact post-goal-creation layout with the full loan use statement up to 200 characters and an amount dropdown instead of the full lending CTA
- **Light Detailed Variant**: A larger layout with borrower name, amount, and country in their own block above a 3-line loan use statement, no orange funding-status tag or refresh button, and a bottom-right arrow (instead of a "View" CTA or "read more" link) that opens the borrower profile modal

<Story of={KvCompactLoanCardStories.AllVariations} />

## Usage Information

KvCompactLoanCard is the primary component for displaying individual loans in compact listing contexts. It integrates with Kiva's loan data structure through GraphQL fragments and provides comprehensive tracking capabilities.

The component requires two essential props: `kvTrackFunction` for analytics and `photoPath` for constructing borrower image URLs. The `loan` object should conform to the GraphQL fragment structure exported by the component.

### When to Use

- In loan listing pages where multiple loans are displayed
- For loan carousels and recommendation sections
- In search results showing matching loans
- On user dashboards displaying saved or bookmarked loans
- Within team pages showing team pick selections
- For matched loan promotions where partner matching is highlighted
- In loan bundle recommendations, where the `light-detailed` variant gives the borrower's identity and a longer loan use statement more room than the standard card allows

### When Not to Use

- When a full loan detail view is needed (use dedicated loan detail components)
- For single, featured loan displays where more detail is appropriate
- In contexts where the dark green background styling doesn't fit
- When loan information is purely informational without lending actions

---

## Behavior

The component handles various interactive states and responds to user actions through emitted events.

### Loading States

The component gracefully handles partial data loading with skeleton placeholders. When `loan` is undefined or partially loaded, placeholder elements appear for the image, loan use text, callouts, and CTA button. This allows the UI to remain stable while data is being fetched.

<Story of={KvCompactLoanCardStories.Loading} />

### Funding Progress

The loan progress bar updates based on `fundedAmount` and `reservedAmount` from `loanFundraisingInfo`. When all shares are reserved, the CTA changes to reflect unavailability. Almost-funded loans display the remaining amount needed prominently.

<Story of={KvCompactLoanCardStories.AlmostFunded} />

### Basket Integration

When a loan is in the user's basket, the component displays an "In basket" state with a modified CTA. The `basketItems` prop should contain the current basket contents to enable this detection.

<Story of={KvCompactLoanCardStories.InBasket} />

### Loan Use Statement and "read more"

In the standard and light views the loan use statement is one sentence ("$375 helps Arfa in Pakistan to buy…") followed, in the light view, by a "read more" link. Without `loanUseMaxLines` the light view behaves as it always has: a 4-line CSS clamp plus the `truncateWordsNumber` word cut, and none of what follows runs. Passing `loanUseMaxLines` opts in to a line cap. Because the link is part of the same paragraph, a plain CSS clamp would hide it on long statements; instead the card measures the rendered paragraph. A statement that fits with its link renders untouched. One that fills the cap on its own is shown whole and the link is dropped. Only a statement that runs past the cap by itself is trimmed word by word until "… read more" ends the last visible line. The fit runs again when the loan changes or the card is resized, so a card that gains room shows more of the statement.

Only the use text is trimmed, never the amount, borrower name or country. If those alone fill the cap, as with `loanUseMaxLines` set to 1 in a 256px card, the link cannot be placed and stays hidden.

`truncateWordsNumber` still applies a word cut before the fit for consumers that want it. It defaults to 0, so with a line cap the lines alone decide unless you set it. Loan cards that never pass `loanUseMaxLines` are untouched by this feature.

<Story of={KvCompactLoanCardStories.LightViewThreeLineLoanUse} />

When the statement fills the cap exactly on its own, it is shown whole and the "read more" link is dropped instead of trimming words to fit it.

<Story of={KvCompactLoanCardStories.LightViewLoanUseFillsThreeLines} />

### Light Detailed Variant Header and Navigation

In the `light-detailed` variant, the borrower name and country truncate independently instead of wrapping or breaking the card's layout, and the loan use statement clamps to 3 lines instead of the word-count truncation and "read more" link the other variants use. The bottom-right arrow replaces the "View" CTA: it's a real link, reachable and activatable by keyboard as well as by click, with an `aria-label` naming whose loan it opens. When `customLoanDetails` is set, it emits `show-loan-details` to open the borrower profile modal instead of navigating.

<Story of={KvCompactLoanCardStories.LightDetailedVariant} />

## Anatomy

The compact loan card consists of several key sections:

- **Borrower Image**: Square thumbnail linking to the full loan page, rendered via KvBorrowerImage
- **Content Area**: Contains business name (optional), loan tags, and loan use description
- **Loan Tags**: Category and attribute pills displayed via KvLoanTag component
- **Loan Callouts**: Additional loan information chips via KvLoanCallouts
- **Progress Section**: KvLoanProgressGroup showing funding status and amount remaining
- **CTA Section**: KvLendCta providing lending actions (Add to basket, View loan, etc.)
- **Refresh Button** (Light View): Optional button for refreshing loan data
- **Team Pick Badge**: Optional overlay for team-selected loans via KvLoanTeamPick
- **Header Block** (Light Detailed Variant): Borrower name, funded amount, and country shown above the loan use statement instead of woven into a single sentence
- **Navigation Arrow** (Light Detailed Variant): Bottom-right link that replaces the "View" CTA and opens the borrower profile modal

---

## Specs

### Card Dimensions

The standard card renders at 380px width with 20px padding in its container. The light view variant renders at 256px width for more compact contexts.

### Image Sizing

- **Standard View**: 100px × 100px borrower image
- **Light View**: Smaller 60px × 60px (7.5 spacing units) image
- **Light Detailed Variant**: 76px × 76px (9.5 spacing units) image, and the card itself uses a 16px corner radius instead of the standard card's 12px

<Story of={KvCompactLoanCardStories.LightViewLoan} />
<Story of={KvCompactLoanCardStories.LightDetailedVariant} />

### Progress and CTA Layout

The bottom section uses a flex layout with the progress information taking available space and the CTA button maintaining a fixed width. The CTA height is constrained to 40px for visual consistency.

---

## Best Practices

<div style={{display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '1.5rem', margin: '2rem 0'}}>
	<div>
		<div style={{marginBottom: '0.5rem'}}>
			<img src="KvCompactLoanCard/do-provide-tracking-function.png" alt="" style={{borderRadius: '8px'}} />
		</div>
		<div style={{fontWeight: 600, marginBottom: '0.5rem', color: '#2e7d32'}}>✓ Do</div>
		<p style={{fontSize: '0.875rem', margin: 0}}>Always provide a valid tracking function via kvTrackFunction prop to enable analytics for user interactions.</p>
	</div>

	<div>
		<div style={{marginBottom: '0.5rem'}}>
			<img src="KvCompactLoanCard/dont-omit-tracking.png" alt="" style={{borderRadius: '8px'}} />
		</div>
		<div style={{fontWeight: 600, marginBottom: '0.5rem', color: '#c62828'}}>✗ Don't</div>
		<p style={{fontSize: '0.875rem', margin: 0}}>Omit the tracking function or use a no-op function in production, as this loses valuable user interaction data.</p>
	</div>

	<div>
		<div style={{marginBottom: '0.5rem'}}>
			<img src="KvCompactLoanCard/do-use-on-dark-backgrounds.png" alt="" style={{borderRadius: '8px'}} />
		</div>
    	<div style={{fontWeight: 600, marginBottom: '0.5rem', color: '#2e7d32'}}>✓ Do</div>
    	<p style={{fontSize: '0.875rem', margin: 0}}>Place the standard card variant on Kiva's signature dark green background (#27593c) for optimal visual contrast.</p>
  	</div>

	<div>
		<div style={{marginBottom: '0.5rem'}}>
			<img src="KvCompactLoanCard/dont-use-standard-on-light.png" alt="" style={{borderRadius: '8px'}} />
		</div>
    	<div style={{fontWeight: 600, marginBottom: '0.5rem', color: '#c62828'}}>✗ Don't</div>
    	<p style={{fontSize: '0.875rem', margin: 0}}>Use the standard card variant on light backgrounds—use the lightView variant instead for lighter contexts.</p>
  	</div>

	<div>
		<div style={{marginBottom: '0.5rem'}}>
			<img src="KvCompactLoanCard/do-use-light-detailed-for-bundles.png" alt="" style={{borderRadius: '8px'}} />
		</div>
    	<div style={{fontWeight: 600, marginBottom: '0.5rem', color: '#2e7d32'}}>✓ Do</div>
    	<p style={{fontSize: '0.875rem', margin: 0}}>Use the light detailed variant where its taller card and larger image fit the layout, such as loan bundle recommendations.</p>
  	</div>

	<div>
		<div style={{marginBottom: '0.5rem'}}>
			<img src="KvCompactLoanCard/dont-mix-light-detailed-with-other-variants.png" alt="" style={{borderRadius: '8px'}} />
		</div>
    	<div style={{fontWeight: 600, marginBottom: '0.5rem', color: '#c62828'}}>✗ Don't</div>
    	<p style={{fontSize: '0.875rem', margin: 0}}>Mix the light detailed variant with the standard or light view in the same grid or carousel; the different card heights break visual alignment.</p>
  	</div>
</div>

---

## Accessibility

- **Image Alt Text**: Borrower images include descriptive alt text with the borrower's name ("Photo of [Name]")
- **Semantic Labels**: Interactive elements (image, tag, loan use, progress) have `aria-label` attributes describing their purpose
- **Link Purpose**: All clickable areas clearly indicate their destination or action
- **Loading States**: Placeholder elements maintain layout stability during data loading
- **Color Contrast**: White card background against dark container ensures WCAG-compliant contrast ratios
- **Focus Management**: Interactive elements are focusable and receive visible focus indicators
- **Keyboard Navigation**: All interactive elements are accessible via keyboard
- **Light Detailed Variant Arrow**: Rendered as a real link with a descriptive `aria-label` (falls back to "this borrower" if the name is empty), so it's reachable and activatable by keyboard as well as by click
- **Truncated Text**: Truncated borrower name and country values carry a `title` attribute so the full value is available on hover

---

## Component Properties + Demo

<Canvas of={KvCompactLoanCardStories.Default} />
<Controls of={KvCompactLoanCardStories.Default} />

---

## Code Examples

### Basic Loan Card

```vue
<template>
  <div style="width: 380px; background: #27593c; padding: 20px;">
    <KvCompactLoanCard
      :loan-id="loan.id"
      :loan="loan"
      :kv-track-function="trackEvent"
      :photo-path="photoPath"
      :show-tags="true"
    />
  </div>
</template>

<script setup>
import { KvCompactLoanCard } from '@kiva/kv-components';

const photoPath = 'https://www.kiva.org/img/';
const loan = { /* loan data from GraphQL */ };
const trackEvent = (category, action, label, value) => {
  // Analytics tracking implementation
};
</script>
```

### Light View with Refresh

```vue
<template>
  <div style="width: 256px;">
    <KvCompactLoanCard
      :loan-id="loan.id"
      :loan="loan"
      :kv-track-function="trackEvent"
      :photo-path="photoPath"
      :show-light-view="true"
      :show-refresh-button="true"
      :truncate-words-number="10"
      @refresh-button="handleRefresh"
    />
  </div>
</template>

<script setup>
import { KvCompactLoanCard } from '@kiva/kv-components';

const handleRefresh = () => {
  // Refresh loan data
};
</script>
```

### Light View with a Three-Line Loan Use

```vue
<template>
  <div style="width: 256px;">
    <KvCompactLoanCard
      :loan-id="loan.id"
      :loan="loan"
      :kv-track-function="trackEvent"
      :photo-path="photoPath"
      :show-light-view="true"
      :loan-use-max-lines="3"
    />
  </div>
</template>
```

### Loan Card with Basket Integration

```vue
<template>
  <div style="width: 380px; background: #27593c; padding: 20px;">
    <KvCompactLoanCard
      :loan-id="loan.id"
      :loan="loan"
      :kv-track-function="trackEvent"
      :photo-path="photoPath"
      :basket-items="basketItems"
      :is-visitor="false"
      :show-tags="true"
      @add-to-basket="handleAddToBasket"
      @remove-from-basket="handleRemoveFromBasket"
    />
  </div>
</template>

<script setup>
import { ref } from 'vue';
import { KvCompactLoanCard } from '@kiva/kv-components';

const basketItems = ref([]);

const handleAddToBasket = (event) => {
  basketItems.value.push({ id: event.loanId, __typename: 'LoanReservation' });
};

const handleRemoveFromBasket = (event) => {
  basketItems.value = basketItems.value.filter(item => item.id !== event.loanId);
};
</script>
```

### Post Goal Variant

```vue
<template>
  <div style="width: 408px;">
    <KvCompactLoanCard
      variant="post-goal"
      :loan-id="loan.id"
      :loan="loan"
      :kv-track-function="trackEvent"
      :photo-path="photoPath"
      :show-tags="true"
      external-links
      custom-href="https://www.kiva.org/lend/1"
      @update:selected-amount="handleSelectedAmount"
    />
  </div>
</template>

<script setup>
import { KvCompactLoanCard } from '@kiva/kv-components';

const handleSelectedAmount = (amount) => {
  // Persist or forward selected lend amount
};
</script>
```

### Light Detailed Variant (Loan Bundles)

```vue
<template>
  <div style="width: 290px;">
    <KvCompactLoanCard
      variant="light-detailed"
      :loan-id="loan.id"
      :loan="loan"
      :kv-track-function="trackEvent"
      :photo-path="photoPath"
      custom-loan-details
      external-links
      custom-amount-lent="25"
      @show-loan-details="openBorrowerProfileModal"
    />
  </div>
</template>

<script setup>
import { KvCompactLoanCard } from '@kiva/kv-components';

const openBorrowerProfileModal = () => {
  // Open the borrower profile modal for this loan
};
</script>
```
