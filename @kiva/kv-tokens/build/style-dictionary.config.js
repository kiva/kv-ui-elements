// Style Dictionary v4 configuration for @kiva/kv-tokens.
// Sources live under ../tokens and platforms emit to ../dist.

export default {
	source: ['tokens/**/*.json'],
	// Platforms are registered incrementally in Tasks 6–8.
	platforms: {},
};
